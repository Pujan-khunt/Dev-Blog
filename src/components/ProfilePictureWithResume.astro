---
import { Image } from "astro:assets";

import Download from "@/svg/Download.astro";
import type { ImageMetadata } from "astro";

interface Props {
  profileImage: ImageMetadata;
  resumeHref: string;
}

const { profileImage, resumeHref } = Astro.props;
---
